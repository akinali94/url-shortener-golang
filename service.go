package main

import(
	"fmt"
)

func getLongUrl(shortUrl string) string {

	//go to database 
}

func generateShortUrl(longUrl string) {
	
	//Generate ID
	//get base58
	//create struct
	//send to db
}